# macOS
.DS_Store

# R
.Rhistory

# Editor / OS noise
Thumbs.db
*.swp

# Working doc: names and critiques real agencies. Public repo — keep local.
reference-notes.md

# Personal GSAP scratch page. Not part of the site.
sandbox.html

# Playwright verification artifacts. Regenerated on demand, not site source.
.playwright-mcp/
chrome-*.png
light-*.png
ticker-*.png
vsl-*.png
ticker-rest.png
home-top.png
work-band.png
capabilities.png
head-check.png

# Raw source photography and logo working files. ~10MB, and the repo is
# public — client photos of real people don't belong in it. Only the
# exported web assets in Assets/web/ ship.
Assets/photos/

# Verification screenshots dropped in the project root.
footer-*.png
audit-inbound.html
